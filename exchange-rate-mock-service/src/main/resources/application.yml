spring:
  application:
    name: exchange-rate-mock-service
  config:
    # Necesario en Spring Boot 3 + Spring Cloud Config para cargar config del Config Server
    import: "optional:configserver:"

server:
  port: 8090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
